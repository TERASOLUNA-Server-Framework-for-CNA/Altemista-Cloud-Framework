/**
 * Private part of the business model of ${businessShortId}
 * (i.e.: the model used internally by the ${businessShortId} business module, but not externally exposed).
 */
package ${package}.${appShortId}.${businessShortId}.model;
