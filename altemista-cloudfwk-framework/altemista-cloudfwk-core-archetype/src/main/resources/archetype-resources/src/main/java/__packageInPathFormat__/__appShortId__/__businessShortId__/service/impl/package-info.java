/**
 * Implementation of both the exposed and the internal services of ${businessShortId}.
 * I.e.: {@code @Service} annotated classes
 */
package ${package}.${appShortId}.${businessShortId}.service.impl;
