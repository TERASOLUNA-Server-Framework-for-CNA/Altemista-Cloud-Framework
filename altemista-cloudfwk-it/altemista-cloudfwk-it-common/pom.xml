<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.altemista.cloudfwk.framework</groupId>
		<artifactId>altemista-cloudfwk-parent</artifactId>
		<version>3.0.0-SNAPSHOT</version>
		<relativePath/>
	</parent>

	<groupId>org.altemista.cloudfwk.it</groupId>
	<artifactId>altemista-cloudfwk-it-common</artifactId>
	<name>altemista-cloudfwk common: integration tests common utilities</name>
	<description>ACF utility classes for integration tests definition and exeuction</description>

	<dependencyManagement>
		<dependencies>

			<!-- == ACF integration tests: bill of materials == -->
			<dependency>
				<groupId>org.altemista.cloudfwk.it</groupId>
				<artifactId>altemista-cloudfwk-it-bom</artifactId>
				<version>${altemista-cloudfwk.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>

		</dependencies>
	</dependencyManagement>

	<dependencies>
	
		<!-- == Begin ACF framework == -->
		<!-- (due to different exclusions on terasoluna-gfw-common, the order of 
			these dependencies is important: web should appear before core or common) -->
		<dependency>
			<groupId>org.altemista.cloudfwk.framework</groupId>
			<artifactId>altemista-cloudfwk-web-conf</artifactId>
		</dependency>
		<dependency>
			<groupId>org.altemista.cloudfwk.framework</groupId>
			<artifactId>altemista-cloudfwk-core</artifactId>
		</dependency>
		<!-- == End ACF framework == -->
		
		<!-- == Begin test dependencies == -->
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- == End test dependencies == -->
		
		<!-- == Begin SeleniumHQ (test) == -->
		<dependency>
			<groupId>org.seleniumhq.selenium</groupId>
			<artifactId>selenium-java</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- == End SeleniumHQ == -->
		
	</dependencies>
	
	<build>
		<plugins>
		
			<!-- This module also builds a test-jar -->
			<plugin>
				<artifactId>maven-jar-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>test-jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			
		</plugins>
	</build>

</project>
