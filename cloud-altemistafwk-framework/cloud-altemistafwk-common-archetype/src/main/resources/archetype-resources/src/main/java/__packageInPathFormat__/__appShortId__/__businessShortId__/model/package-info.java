/**
 * Business model of ${businessShortId} (i.e.: the model actually exposed by the ${businessShortId} business module).
 * This model contains the model used by the service interfaces, but can include additional classes as well.
 */
package ${package}.${appShortId}.${businessShortId}.model;
