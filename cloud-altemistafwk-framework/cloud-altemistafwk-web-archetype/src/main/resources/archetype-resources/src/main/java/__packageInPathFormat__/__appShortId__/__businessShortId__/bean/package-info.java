/**
 * Web-tier view beans of ${businessShortId}.
 * The view beans are tied to the web boundary, usually because they define how they are rendered outside the module
 * (e.g.: JSON or XML serialization related annotations).
 * Also, enforces decoupling between the business module domain and the web boundary.
 */
package ${package}.${appShortId}.${businessShortId}.bean;
