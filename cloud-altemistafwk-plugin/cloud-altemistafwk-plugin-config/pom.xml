<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>cloud.altemista.fwk.framework</groupId>
		<artifactId>cloud-altemistafwk-recommended-plugins</artifactId>
		<version>3.0.0-SNAPSHOT</version>
		<relativePath />
	</parent>
	
	<groupId>cloud.altemista.fwk.plugin</groupId>
	<artifactId>cloud-altemistafwk-plugin-config</artifactId>
	
	<properties>
		<!-- == ACF version == -->
		<cloud-altemistafwk.version>${project.version}</cloud-altemistafwk.version>
		
		<!-- == YamlBeans version == -->
		<yamlbeans.version>1.09</yamlbeans.version>
		
		<!-- == Maven version ==
			The Maven version here is tentative; the dependency is just to support ValueDto#versionRange.
			The plug-in core using this configuration should provide the proper version
			(e.g.: provided by the Eclipse Maven plug-in, etc.)
		-->
		<maven.version>3.3.9</maven.version>
	</properties>
	
	<dependencyManagement>
		<dependencies>
			
			<!-- == ACF framework: bill of materials == -->
			<dependency>
				<groupId>cloud.altemista.fwk.framework</groupId>
				<artifactId>cloud-altemistafwk-bom</artifactId>
				<version>${cloud-altemistafwk.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			
		</dependencies>
	</dependencyManagement>
	
	<dependencies>
	
		<!-- == Begin YamlBeans == -->
		<dependency>
			<groupId>com.esotericsoftware.yamlbeans</groupId>
			<artifactId>yamlbeans</artifactId>
			<version>${yamlbeans.version}</version>
		</dependency>
		<!-- == End YamlBeans == -->
		
		<!-- == Begin Apache Maven (for ValueDto#versionRange) == -->
		<dependency>
			<groupId>org.apache.maven</groupId>
			<artifactId>maven-artifact</artifactId>
			<version>${maven.version}</version>
			<scope>provided</scope>
		</dependency>
		<!-- == End Apache Maven == -->
		
		<!-- == Begin Apache Commons (for utility classes) == -->
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-lang3</artifactId>
		</dependency>
		<!-- == End Apache Commons == -->
		
		<!-- == Begin test dependencies == -->
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- == End test dependencies -->

	</dependencies>

</project>